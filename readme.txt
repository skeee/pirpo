1) Create and move to 'build' directory
2) Run 'cmake3 ..'
3) Run 'make'
4) Move to 'src' directory
5) Run 'webservice'
6) Open browser and go to 'http://127.0.0.1:9080/convert?from=c&to=f&value=0.0', etc.

Pistache library must be installed to build project.
